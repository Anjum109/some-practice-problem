// let arr = ['html', 'css', 'tailwind', 'bootstrap', 'js'];
// for (let i = 0; i < arr.length; i++) {
//     console.log(arr[i]);
// }

function isJavaScriptFile(string) {

    if (string.endsWith('.js') === true) {
        return true;
    } else if {
        return false;
    }
    else (typeof string !== 'string') {
        return 'enter a valid number';
    }
}
console.log(isJavaScriptFile(string.js));